# Only needed when uploading or asking the assistant.
OPENAI_API_KEY=
# API_KEY is also accepted as an alias, matching the assignment.
OPENAI_VECTOR_STORE_ID=
# Set a model available in your account that supports Responses + File Search.
OPENAI_MODEL=
DATA_DIR=data
